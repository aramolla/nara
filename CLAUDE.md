# CLAUDE.md  

## 프로젝트 개요  
나라장터 사이트 공고 법령 위반 판정  
- 목표 : 공고 1건 ->  24개 항목 위반여부(0/1) + 근거문장  
- 평가 기준 : Macro F1(해당 평가에는 24 항목 위반 여부 열만 반영)  


## 규칙  
- 외부 데이터 수집 및 활용 일체 금지(ex웹 크롤링, API, 공공 데이터등 )  
- 법령 판정 기준은 배포된 법령패키지 스냅샷 고정(최신 법령 검색/반영 금지)    
- 고정 LLM 기반 추론 파이프라인을 사용해야하며 1회 이상 호출 필수  
- 고정 LLM : google/gemma-4-26B-A4B-it  
- 고정 임베딩 모델 : BAAI/bge-m3  
- 고정 모델 이외의 별도 분류기, 파인튜닝, 어댑터 학습 금지  
- 모델 컨텍스트 상한 32,768 (베이스라인은 16,384로 축소 로드 — 실행시간 단축 목적, 확장 여지 있음)


## 제출 폴더 구조  
제출 폴더 구조는 아래 구조를 반드시 지켜야 한다.

submit.zip(폴더명 예시)
├── script.py          # 필수
├── requirements.txt    # 선택
└── model/              # 선택, RAG 인덱스, 설정파일 등 정적 자산만  

- 고정 모델은 평가 서버에 준비되어 있고 따로 포함하지 않는다.  


##  프로젝트 디렉터리 구조 규칙  
- script.py, requirements.txt, model/ 은 최상위 고정, 각 1개 유지(제출 구조와 유사하게 유지하기 위함)  
- baseline/ 은 대회 원본 그대로 보관한다. 수정금지  
- 모든 실험은 prep/ 에서 진행  


## 명령어  
- mock 검증 : python script.py --mock  


## 참고 문서  
- 데이터 명세 : docs/data-spec.md 참고

